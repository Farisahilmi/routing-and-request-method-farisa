<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .users-container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }
        
        .users-table {
            width: 100%;
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.08);
            overflow: hidden;
            border: 1px solid #eaeaea;
        }
        
        .table-header {
            background: linear-gradient(135deg, #2c5aa0, #3a7bd5);
            color: white;
            padding: 20px;
        }
        
        .table-header h2 {
            margin: 0;
            font-size: 24px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
        }
        
        th {
            background: #f8f9fa;
            padding: 16px 20px;
            text-align: left;
            font-weight: 600;
            color: #495057;
            border-bottom: 2px solid #e9ecef;
        }
        
        td {
            padding: 16px 20px;
            border-bottom: 1px solid #e9ecef;
            color: #666;
        }
        
        tr:hover {
            background: #f8f9fa;
            transition: background 0.3s ease;
        }
        
        .action-buttons {
            display: flex;
            gap: 8px;
        }
        
        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            font-size: 14px;
        }
        
        .btn-update {
            background: #ffc107;
            color: black;
        }
        
        .btn-update:hover {
            background: #e0a800;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(255, 193, 7, 0.3);
        }
        
        .btn-delete {
            background: #dc3545;
            color: white;
        }
        
        .btn-delete:hover {
            background: #c82333;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(220, 53, 69, 0.3);
        }
        
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #666;
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.08);
        }
        
        .register-promo {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 12px;
            margin-top: 30px;
            text-align: center;
        }
        
        .register-promo h3 {
            margin: 0 0 15px 0;
            font-size: 20px;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
            padding: 12px 24px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(40, 167, 69, 0.4);
        }

        .user-role {
            display: inline-flex;
            align-items: center;
            gap: 5px;
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 500;
        }
        
        .role-admin {
            background: #dc3545;
            color: white;
        }
        
        .role-customer {
            background: #28a745;
            color: white;
        }
    </style>
</head>
<body>
    <%- include('nav') %>
    <%- include('notification') %>
    <div class="users-container">
        <div class="page-header">
            <h1 style="margin: 0; color: #2c5aa0; display: flex; align-items: center; gap: 10px;">
                <i class="fas fa-users"></i> <%= title %>
            </h1>
            <a href="/users/register" class="btn-primary">
                <i class="fas fa-user-plus"></i> Add New User
            </a>
        </div>

        <% if (users && users.length > 0) { %>
            <div class="users-table">
                <div class="table-header">
                    <h2><i class="fas fa-user-cog"></i> User Management</h2>
                </div>
                
                <table>
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Username</th>
                            <th>Email</th>
                            <th>Role</th>
                            <th>Created At</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach(function(user) { %>
                            <tr>
                                <td style="font-weight: bold; color: #2c5aa0;">
                                    <i class="fas fa-id-card"></i> <%= user.id %>
                                </td>
                                <td style="font-weight: 500;">
                                    <i class="fas fa-user"></i> <%= user.username %>
                                </td>
                                <td>
                                    <i class="fas fa-envelope"></i> <%= user.email %>
                                </td>
                                <td>
                                    <span class="user-role role-<%= user.role %>">
                                        <i class="fas fa-<%= user.role === 'admin' ? 'crown' : 'user' %>"></i>
                                        <%= user.role %>
                                    </span>
                                </td>
                                <td>
                                    <i class="fas fa-calendar"></i> <%= new Date(user.createdAt).toLocaleDateString() %>
                                </td>
                                <td>
                                    <div class="action-buttons">
                                        <a href="/users/update/<%= user.id %>" class="btn btn-update">
                                            <i class="fas fa-edit"></i> Update
                                        </a>
                                        <button type="button" onclick="deleteUser('<%= user.id %>')" class="btn btn-delete">
                                            <i class="fas fa-trash"></i> Delete
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <div class="empty-state">
                <i class="fas fa-users fa-3x" style="color: #ccc; margin-bottom: 20px;"></i>
                <h3>No Users Found</h3>
                <p>There are no users registered in the system yet.</p>
                <a href="/users/register" class="btn-primary" style="margin-top: 20px;">
                    <i class="fas fa-user-plus"></i> Register First User
                </a>
            </div>
        <% } %>
        
        <div class="register-promo">
            <h3><i class="fas fa-rocket"></i> Ready to Grow?</h3>
            <p>Add more users to your system and manage them efficiently.</p>
            <a href="/users/register" class="btn-primary" style="background: rgba(255,255,255,0.2); border: 2px solid white;">
                <i class="fas fa-star"></i> Get Started
            </a>
        </div>
    </div>

    <script>
        async function deleteUser(id) {
            if (!confirm('Are you sure you want to delete this user?')) return;
            
            try {
                const response = await fetch('/users/' + id, {
                    method: 'DELETE'
                });
                const result = await response.json();
                
                if (result.status === 'success') {
                    showNotification('success', 'User Deleted', result.message);
                    setTimeout(() => {
                        window.location.reload();
                    }, 1500);
                } else {
                    showNotification('error', 'Delete Failed', result.message || 'Failed to delete user.');
                }
            } catch (error) {
                showNotification('error', 'Error', 'Error deleting user');
            }
        }
    </script>
</body>
</html>