<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
        .product-detail {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .product-header {
            display: flex;
            gap: 30px;
            margin-bottom: 30px;
        }
        .product-image {
            flex: 1;
            background: #f5f5f5;
            border-radius: 8px;
            padding: 20px;
            text-align: center;
            min-height: 300px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .product-info {
            flex: 2;
        }
        .product-name {
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 10px;
            color: #333;
        }
        .product-price {
            font-size: 32px;
            font-weight: bold;
            color: #2c5aa0;
            margin-bottom: 15px;
        }
        .product-category {
            display: inline-block;
            background: #007bff;
            color: white;
            padding: 6px 12px;
            border-radius: 15px;
            font-size: 14px;
            margin-bottom: 15px;
        }
        .product-stock {
            font-size: 16px;
            margin-bottom: 20px;
        }
        .in-stock {
            color: #28a745;
            font-weight: bold;
        }
        .out-of-stock {
            color: #dc3545;
            font-weight: bold;
        }
        .product-description {
            line-height: 1.6;
            color: #555;
            margin-bottom: 20px;
        }
        .action-buttons {
            display: flex;
            gap: 10px;
            margin-top: 30px;
        }
        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            text-decoration: none;
            display: inline-block;
            font-weight: bold;
            cursor: pointer;
        }
        .btn-primary {
            background: #007bff;
            color: white;
        }
        .btn-secondary {
            background: #6c757d;
            color: white;
        }
        .btn-success {
            background: #28a745;
            color: white;
        }
        .api-info {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 4px;
            padding: 15px;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <%- include('nav') %>
    <%- include('notification') %>
    <%- include('head') %>
    <div class="product-detail">
        <div class="product-header">
            <div class="product-image">
                <% if (product.image && product.image !== '/images/default.jpg') { %>
                    <img src="<%= product.image %>" alt="<%= product.name %>" style="max-width: 100%; max-height: 400px;">
                <% } else { %>
                    <div style="text-align: center; color: #666;">
                        <div style="font-size: 48px; margin-bottom: 10px;">🖼️</div>
                        <div>Product Image</div>
                    </div>
                <% } %>
            </div>
            
            <div class="product-info">
                <h1 class="product-name"><%= product.name %></h1>
                <div class="product-price">$<%= product.price %></div>
                <span class="product-category"><%= product.category %></span>
                
                <div class="product-stock <%= product.stock > 0 ? 'in-stock' : 'out-of-stock' %>">
                    <%= product.stock > 0 ? `✅ In Stock (${product.stock} available)` : '❌ Out of Stock' %>
                </div>
                
                <div class="product-description">
                    <h3>Description</h3>
                    <p><%= product.description %></p>
                </div>
                
                <div class="action-buttons">
                    <a href="/products" class="btn btn-secondary">← Back to Products</a>
                    <a href="/api/products/<%= product.id %>" class="btn btn-success" target="_blank">View API Data</a>
                    <% if (product.stock > 0) { %>
                        <button class="btn btn-primary">Add to Cart</button>
                    <% } %>
                </div>
            </div>
        </div>
        
        <div class="api-info">
            <h3>API Endpoint</h3>
            <p>JSON data available at: <code>/api/products/<%= product.id %></code></p>
            <a href="/api/products/<%= product.id %>" target="_blank">View Raw JSON</a>
        </div>
    </div>
</body>
</html>